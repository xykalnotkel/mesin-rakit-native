<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.audio.output" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/TemaMesinRakit"
        android:supportsRtl="false">

        <!-- pintu masuk: tema standar, isi seminimal mungkin -->
        <activity
            android:name="id.mesinrakit.BukaActivity"
            android:theme="@style/TemaBiasa"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- layar game -->
        <!-- catatan: orientasi lanskap dan configChanges sengaja tidak
             ditulis di sini. Keduanya dipasang lewat kode supaya bisa
             ditangkap kalau ada HP yang menolaknya (lihat MainActivity). -->
        <activity
            android:name="id.mesinrakit.MainActivity"
            android:exported="false" />

        <!-- alat uji mandiri, muncul sebagai ikon kedua di laci aplikasi -->
        <activity
            android:name="id.mesinrakit.UjiActivity"
            android:theme="@style/TemaBiasa"
            android:exported="true"
            android:label="UJI MESIN">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- layar laporan error, proses terpisah supaya tetap muncul
             walaupun proses utama sudah dimatikan sistem -->
        <activity
            android:name="id.mesinrakit.LaporActivity"
            android:process=":lapor"
            android:exported="false"
            android:label="Laporan Error"
            android:theme="@style/TemaLapor" />
    </application>
</manifest>
